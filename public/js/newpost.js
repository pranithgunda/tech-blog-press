const createNewPostEl = document.getElementById('createNewPost');

// event listener to create new post
createNewPostEl.addEventListener('click',(event)=>{
    console.log('Create button clicked for post creation');
})