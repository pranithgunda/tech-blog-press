const feedbackBtnEl = document.getElementById('feedbackBtn');

// event listener to listen for user submitted feedback
feedbackBtnEl.addEventListener('click',(event)=>{
    console.log('feeback button clicked');
})